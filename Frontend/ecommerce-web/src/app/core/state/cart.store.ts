import { Injectable } from '@angular/core';

/**
 * @deprecated Store temporal - Será reemplazado por la nueva implementación de Clean Architecture
 * Este store existe solo para evitar errores de compilación durante la migración
 */
@Injectable({
  providedIn: 'root'
})
export class CartStore {
  // Implementación temporal vacía
}